{
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --minify --bundle --platform=browser --format=esm --outfile=dist/index.js && esbuild src/distro.ts --external:./index.js --minify --bundle --platform=browser --format=esm --outfile=dist/distro.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  }
}
